# AI Job Matcher - Environment Configuration
# Copy this file to .env and fill in your values

# ─── Database ──────────────────────────────────────────────
# Use DB_HOST=db         when running via Docker Compose
# Use DB_HOST=127.0.0.1  when running backend locally (outside Docker)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=job_matcher
DB_USER=job_matcher
DB_PASSWORD=JobMatcher2024!
DATABASE_URL=postgresql://job_matcher:JobMatcher2024!@127.0.0.1:5432/job_matcher

# ─── Google Gemini AI (CV Enhancement) ─────────────────────
# Get your API key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# ─── OpenAI (legacy scripts only) ─────────────────────────
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# ─── Telegram Notifications (optional) ────────────────────
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ─── Google Docs (optional) ───────────────────────────────
RESUME_DOC_ID=
RESUME_FOLDER_ID=
